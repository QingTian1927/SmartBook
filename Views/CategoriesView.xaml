<Page x:Class="SmartBook.Views.CategoriesView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:SmartBook.Views"
      xmlns:views="clr-namespace:SmartBook.Views.Components"
      mc:Ignorable="d"
      Loaded="Page_Loaded"
      Title="SmartBook - Categories"
      >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <views:SidebarView CurrentUsername="{Binding CurrentUsername}" />

        <!-- Main content -->
        <Grid Grid.Column="1" Background="White">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />  <!-- Search bar -->
                <RowDefinition Height="*" />     <!-- Category grid -->
            </Grid.RowDefinitions>

            <!-- Search Bar -->
            <WrapPanel Grid.Row="0" Margin="20" VerticalAlignment="Center" HorizontalAlignment="Left">
                <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0 0 5 0" />
                <TextBox x:Name="SearchBox" Width="300" Margin="0 0 20 0" TextChanged="SearchBox_OnTextChanged" />
            </WrapPanel>

            <!-- Category Grid -->
            <ScrollViewer Grid.Row="1" Margin="20" HorizontalScrollBarVisibility="Disabled"
                          VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="CategoryGrid">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" HorizontalAlignment="Left" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Padding="0" Margin="10" Width="220" Height="140"
                                    Background="#F9F9F9" BorderBrush="#DDD" BorderThickness="1"
                                    Cursor="Hand" Click="CategoryItem_Click">
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="8">
                                    <TextBlock Text="{Binding Name}"
                                               FontWeight="Bold" FontSize="16"
                                               TextAlignment="Center"
                                               TextWrapping="Wrap"
                                               VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding BookCount, StringFormat={}{0} books}"
                                               FontSize="12"
                                               Foreground="Gray"
                                               Margin="0,5,0,0"
                                               TextAlignment="Center" />
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>